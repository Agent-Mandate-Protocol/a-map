{
  "name": "amap-registry",
  "version": "0.0.1",
  "description": "A-MAP registry Cloudflare Worker — registry.agentmandateprotocol.dev",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@agentmandateprotocol/core": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  },
  "engines": { "node": ">=18.0.0" }
}
